# This is a basic workflow to help you get started with Actions

name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: buban01/githubactiontest@main
      - name: Set up JDK 11
       
      - name: Build with Maven
        run: mvn --batch-mode --update-snapshots verify
